{
  "name": "qqs-app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.base.json --noEmit"
  },
  "devDependencies": {
    "expo-router": "~57.0.22",
    "typescript": "^5.9.2"
  }
}
